# Personal configuration — never commit this
protect-on-mac.conf

# Local VM artifacts (in case anyone keeps them in-repo)
vm-data/
*.qcow2
*.fd

# macOS noise
.DS_Store

# Python bytecode
__pycache__/
*.pyc

# Editor swap files
*.swp
*~

# Superseded experiment — the ustated-shim.js approach replaced it
usd-shim.py
